<controlzEx:PopupEx x:Class="Accelerider.Windows.Views.SettingsPopup"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controlzEx="clr-namespace:ControlzEx;assembly=MaterialDesignThemes.Wpf"
                    xmlns:ctrls="clr-namespace:Accelerider.Windows.Controls"
                    xmlns:mvvm="http://prismlibrary.com/"
                    mvvm:ViewModelLocator.AutoWireViewModel="True"
                    StaysOpen="True" AllowsTransparency="True">
    <Border Margin="2" Background="White" BorderThickness="1" CornerRadius="2"
            Effect="{StaticResource MaterialDesignShadowDepth2}">
        <StackPanel>
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Ellipse Grid.Column="0" Height="50" Width="50">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="../Assets/Images/HeadImage.png" />
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Grid.Column="2" Text="Laplace's Demon" VerticalAlignment="Center"/>
            </Grid>
            <StackPanel Margin="0 0 0 8" >
                <!-- open dialog -->
                <ctrls:SettingItem IconKind="AccountSettings" Label="Change profile" Command="{Binding ChangeProfileCommand}"/>
                <Rectangle Height="2" Margin="0 8" Fill="{StaticResource LightGreyBackgoundBrush}"/>
                <!-- sub menu -->
                <ctrls:SettingItem IconKind="Application" Label="Application"/>
                <!-- open dialog or sub menu-->
                <ctrls:SettingItem IconKind="CommentProcessingOutline" Label="Notifications"/>
                <!-- sub menu -->
                <ctrls:SettingItem IconKind="Translate" Label="Language"/>
                <!-- open dialog -->
                <ctrls:SettingItem IconKind="Settings" Label="Open other settings" Command="{Binding OpenOtherSettingsCommand}"/>
                <!-- open web -->
                <ctrls:SettingItem IconKind="HelpCircleOutline" Label="Help" Command="{Binding HelpCommand}"/>
                <Rectangle Height="2" Margin="0 8" Fill="{StaticResource LightGreyBackgoundBrush}"/>
                <!-- open menu -->
                <ctrls:SettingItem IconKind="InformationVariant" Label="About"/>
                <!-- message queue -->
                <ctrls:SettingItem IconKind="ArrowUpBoldCircleOutline" Label="Check for updates" Command="{Binding CheckUpdateCommand}"/>
                <!-- open web -->
                <ctrls:SettingItem IconKind="Web" Label="Official site" Command="{Binding OpenOfficialSiteCommand}"/>
                <Rectangle Height="2" Margin="0 8" Fill="{StaticResource LightGreyBackgoundBrush}"/>
                <!-- open EnteringWindow and closes MainWindow -->
                <ctrls:SettingItem IconKind="Logout" Label="Log out" Command="{Binding SignOutCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</controlzEx:PopupEx>
