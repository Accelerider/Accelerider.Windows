<Window x:Class="Accelerider.Windows.EnteringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:v="clr-namespace:Accelerider.Windows.Views"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Title="EnteringWindow" Height="300" Width="430"
        Style="{StaticResource EnteringWindowStyle}">

    <materialDesign:DialogHost Identifier="EnteringDialog" SnackbarMessageQueue="{Binding MessageQueue}">
        <Grid Background="White">
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" Focusable="False"
                    Style="{StaticResource MaterialDesignToolButton}"
                    WindowChrome.IsHitTestVisibleInChrome="True"
                    Margin="0 2 5 0">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <ei:CallMethodAction TargetObject="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" MethodName="Close" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <materialDesign:PackIcon Kind="WindowClose"></materialDesign:PackIcon>
            </Button>
            <Grid Margin="60 30">
                <v:SignInView/>
            </Grid>
            <materialDesign:Snackbar MessageQueue="{Binding MessageQueue}" />
        </Grid>
    </materialDesign:DialogHost>
</Window>
